# Dependencies
node_modules
apps/**/node_modules

# Build output
dist
*.tsbuildinfo

# Cloudflare / wrangler
.wrangler
apps/**/.wrangler
apps/web/worker-configuration.d.ts
# Local Cloudflare credentials for scripts/workers-logs.mjs + the
# diagnose-worker-logs skill. Keep env.example tracked; ignore real values.
.cloudflare/*
!.cloudflare/env.example

# Local env / secrets
.env
apps/**/.env
.dev.vars
apps/**/.dev.vars

# Test artifacts
.playwright-cli
playwright-report
test-results

# OS
.DS_Store

# Stale local artifacts: abandoned CMS, removed Vercel integration, agent worktrees
.vercel
apps/**/.vercel
apps/cms/
.claude/worktrees/

# TanStack dev-server temp files
apps/web/.tanstack/
gui-test-screenshots/
